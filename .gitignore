# ============ 数据：账号 + 书签 + 备份（绝不上传） ============
*.db
*.sqlite
*.sqlite3
backups/

# ============ 运行时状态 ============
logs/
*.pid
*.log

# ============ Node 依赖（包体太大，传 package.json 即可） ============
node_modules/

# ============ macOS / 编辑器 / 历史备份 ============
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~
*.backup_*

# ============ 环境变量 / 凭证 ============
.env
.env.*
*.pem
*.key
